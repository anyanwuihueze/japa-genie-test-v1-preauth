# Settings for Firebase App Hosting
# https://firebase.google.com/docs/app-hosting/configure
runConfig:
  # Automatically spin up more instances in response to traffic.
  # For a Spark plan, a max of 1-2 instances is recommended to manage costs.
  maxInstances: 1

# After deploying, you will need to set up your environment variables
# in Google Cloud Secret Manager and grant the App Hosting service account
# access to them. The variables your app uses include:
#
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY
# - NEXT_PUBLIC_GEMINI_API_KEY
# - NEXT_PUBLIC_GROQ_API_KEY
# - CRON_SECRET
# - PAYSTACK_SECRET_KEY
# - STRIPE_WEBHOOK_SECRET
