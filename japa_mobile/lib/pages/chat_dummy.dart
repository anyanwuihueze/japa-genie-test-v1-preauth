import 'package:flutter/material.dart';
class ChatDummy extends StatelessWidget {
  const ChatDummy({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Chat page'));
}
